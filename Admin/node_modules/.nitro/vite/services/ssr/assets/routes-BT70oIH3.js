import { d as fetchStats, i as fetchActivity, o as fetchAdminVendors, s as fetchCategories } from "./endpoints-NEJuUwSh.js";
import { n as useAuth } from "./auth-C9p9fr_1.js";
import { a as SolidButton, c as Thumb, i as PanelHead, l as AdminShell, n as PageHead, o as StatCard, r as Panel, s as StatusPill, u as cn } from "./ui-fTbEUKQg.js";
import { t as fallbackVendorImage } from "./ngila-D2EB65r3.js";
import { Link } from "@tanstack/react-router";
import { jsx, jsxs } from "react/jsx-runtime";
import { useQuery } from "@tanstack/react-query";
//#region src/routes/index.tsx?tsr-split=component
var dot = {
	"vendor-added": "bg-ember",
	"vendor-verified": "bg-moss",
	"vendor-suspended": "bg-clay",
	"vendor-reinstated": "bg-moss",
	"claim-rejected": "bg-clay",
	"vendor-claimed": "bg-amber",
	default: "bg-ink"
};
function Overview() {
	const { status, user } = useAuth();
	const enabled = status === "authenticated";
	const { data: stats } = useQuery({
		queryKey: ["admin-stats"],
		queryFn: fetchStats,
		enabled
	});
	const { data: activity } = useQuery({
		queryKey: ["admin-activity"],
		queryFn: () => fetchActivity(8),
		enabled
	});
	const { data: vendors } = useQuery({
		queryKey: ["admin-vendors"],
		queryFn: fetchAdminVendors,
		enabled
	});
	const { data: categories } = useQuery({
		queryKey: ["categories"],
		queryFn: fetchCategories,
		enabled
	});
	const statCards = stats ? [
		{
			label: "Total vendors",
			value: stats.totalVendors.toLocaleString(),
			note: "across the network",
			tone: "moss"
		},
		{
			label: "Pending verification",
			value: stats.pendingVerification.toLocaleString(),
			note: "awaiting review",
			tone: "ember",
			alert: stats.pendingVerification > 0
		},
		{
			label: "Active users",
			value: stats.activeUsers.toLocaleString(),
			note: "customers, vendors & admins",
			tone: "moss"
		}
	] : [];
	const sortedCategories = [...categories ?? []].sort((a, b) => b.vendorCount - a.vendorCount).slice(0, 7);
	const max = Math.max(1, ...sortedCategories.map((c) => c.vendorCount));
	const recentVendors = (vendors ?? []).slice(0, 5);
	return /* @__PURE__ */ jsxs(AdminShell, { children: [
		/* @__PURE__ */ jsx(PageHead, {
			kicker: (/* @__PURE__ */ new Date()).toLocaleDateString("en-ZA", {
				weekday: "long",
				day: "numeric",
				month: "short",
				year: "numeric"
			}),
			title: `Good to see you, ${user?.firstName ?? ""}.`,
			actions: /* @__PURE__ */ jsx(Link, {
				to: "/verification",
				children: /* @__PURE__ */ jsxs(SolidButton, { children: ["Open queue · ", stats?.pendingVerification ?? 0] })
			})
		}),
		/* @__PURE__ */ jsx("div", {
			className: "grid grid-cols-2 gap-3 lg:grid-cols-3",
			children: statCards.map((s, i) => /* @__PURE__ */ jsx(StatCard, {
				...s,
				delay: 100 + i * 50
			}, s.label))
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "grid gap-6 lg:grid-cols-3",
			children: [/* @__PURE__ */ jsxs(Panel, {
				className: "lg:col-span-2 p-5",
				delay: 250,
				children: [/* @__PURE__ */ jsx(PanelHead, {
					title: "Vendors by category",
					kicker: `${categories?.length ?? 0} active categories`
				}), /* @__PURE__ */ jsxs("div", {
					className: "flex h-48 items-end gap-3 pt-4",
					children: [sortedCategories.map((c, i) => /* @__PURE__ */ jsxs("div", {
						className: "flex flex-1 flex-col items-center gap-2",
						children: [
							/* @__PURE__ */ jsx("span", {
								className: "font-mono text-[10px] text-mute",
								children: c.vendorCount
							}),
							/* @__PURE__ */ jsx("div", {
								className: cn("w-full rounded-t-md animate-rise", c.vendorCount === max ? "bg-ember" : "bg-ink/80"),
								style: {
									height: `${c.vendorCount / max * 140}px`,
									animationDelay: `${300 + i * 40}ms`
								}
							}),
							/* @__PURE__ */ jsx("span", {
								className: "label-mono truncate",
								children: c.name
							})
						]
					}, c.id)), sortedCategories.length === 0 ? /* @__PURE__ */ jsx("p", {
						className: "text-mute",
						children: "No categories yet."
					}) : null]
				})]
			}), /* @__PURE__ */ jsxs(Panel, {
				className: "p-5",
				delay: 300,
				children: [/* @__PURE__ */ jsx(PanelHead, {
					title: "Activity",
					kicker: "Across all regions"
				}), /* @__PURE__ */ jsxs("ul", {
					className: "space-y-4",
					children: [(activity ?? []).map((a) => /* @__PURE__ */ jsxs("li", {
						className: "flex gap-3",
						children: [/* @__PURE__ */ jsx("span", { className: cn("mt-1.5 size-2 shrink-0 rounded-full", dot[a.tone] ?? dot["default"]) }), /* @__PURE__ */ jsxs("div", {
							className: "text-[13px] leading-snug",
							children: [
								/* @__PURE__ */ jsx("span", {
									className: "font-medium",
									children: a.who
								}),
								" ",
								a.what,
								/* @__PURE__ */ jsx("p", {
									className: "font-mono text-[10px] text-mute",
									children: a.when
								})
							]
						})]
					}, a.id)), (activity ?? []).length === 0 ? /* @__PURE__ */ jsx("p", {
						className: "text-mute",
						children: "No activity yet."
					}) : null]
				})]
			})]
		}),
		/* @__PURE__ */ jsxs(Panel, {
			className: "p-5",
			delay: 350,
			children: [/* @__PURE__ */ jsx(PanelHead, {
				title: "Recently updated vendors",
				right: /* @__PURE__ */ jsx(Link, {
					to: "/vendors",
					className: "text-[13px] text-ember hover:underline",
					children: "View all →"
				})
			}), /* @__PURE__ */ jsxs("div", {
				className: "divide-y divide-line",
				children: [recentVendors.map((v) => /* @__PURE__ */ jsxs("div", {
					className: "flex items-center gap-4 py-3",
					children: [
						/* @__PURE__ */ jsx(Thumb, {
							src: v.image ?? fallbackVendorImage,
							alt: v.name,
							className: "size-11"
						}),
						/* @__PURE__ */ jsxs("div", {
							className: "min-w-0 flex-1",
							children: [/* @__PURE__ */ jsx("p", {
								className: "truncate text-[14px] font-medium",
								children: v.name
							}), /* @__PURE__ */ jsxs("p", {
								className: "truncate font-mono text-[11px] text-mute",
								children: [
									v.category,
									" · ",
									v.location
								]
							})]
						}),
						/* @__PURE__ */ jsxs("span", {
							className: "hidden font-mono text-[12px] sm:block",
							children: ["★ ", v.rating.toFixed(1)]
						}),
						/* @__PURE__ */ jsx(StatusPill, { status: v.status })
					]
				}, v.id)), recentVendors.length === 0 ? /* @__PURE__ */ jsx("p", {
					className: "p-4 text-center text-mute",
					children: "No vendors yet."
				}) : null]
			})]
		})
	] });
}
//#endregion
export { Overview as component };
