import { S as unsuspendVendor, T as ApiError, b as suspendVendor, o as fetchAdminVendors } from "./endpoints-NEJuUwSh.js";
import { n as useAuth } from "./auth-C9p9fr_1.js";
import { t as Route } from "./vendors-CkOT_vwZ.js";
import { c as Thumb, l as AdminShell, n as PageHead, r as Panel, s as StatusPill, t as GhostButton, u as cn } from "./ui-fTbEUKQg.js";
import { t as fallbackVendorImage } from "./ngila-D2EB65r3.js";
import { useState } from "react";
import { jsx, jsxs } from "react/jsx-runtime";
import { useMutation, useQuery, useQueryClient } from "@tanstack/react-query";
import { toast } from "sonner";
//#region src/routes/vendors.tsx?tsr-split=component
var filters = [
	"All",
	"Verified",
	"Pending",
	"Suspended"
];
var filterLabel = {
	All: "All",
	Verified: "Verified",
	Pending: "Pending",
	Suspended: "Suspended"
};
function Vendors() {
	const { status } = useAuth();
	const queryClient = useQueryClient();
	const { q: initialQ } = Route.useSearch();
	const [f, setF] = useState("All");
	const [q, setQ] = useState(initialQ ?? "");
	const { data: vendors, isLoading } = useQuery({
		queryKey: ["admin-vendors"],
		queryFn: fetchAdminVendors,
		enabled: status === "authenticated"
	});
	const onError = (err) => toast.error(err instanceof ApiError ? err.message : "That didn't work.");
	const invalidate = () => void queryClient.invalidateQueries({ queryKey: ["admin-vendors"] });
	const suspendMutation = useMutation({
		mutationFn: suspendVendor,
		onSuccess: () => {
			toast.success("Vendor suspended.");
			invalidate();
		},
		onError
	});
	const unsuspendMutation = useMutation({
		mutationFn: unsuspendVendor,
		onSuccess: () => {
			toast.success("Vendor reinstated.");
			invalidate();
		},
		onError
	});
	const list = (vendors ?? []).filter((v) => (f === "All" || v.status === f) && (v.name + v.location + v.category).toLowerCase().includes(q.toLowerCase()));
	return /* @__PURE__ */ jsxs(AdminShell, { children: [
		/* @__PURE__ */ jsx(PageHead, {
			kicker: `${vendors?.length ?? 0} listings`,
			title: "Vendors"
		}),
		/* @__PURE__ */ jsxs("div", {
			className: "flex flex-wrap items-center gap-2",
			children: [filters.map((x) => /* @__PURE__ */ jsx("button", {
				onClick: () => setF(x),
				className: cn("rounded-full px-3 py-1.5 text-[12px] font-medium transition", f === x ? "bg-ink text-paper" : "bg-surface/70 text-mute ring-1 ring-line hover:text-ink"),
				children: filterLabel[x]
			}, x)), /* @__PURE__ */ jsx("input", {
				value: q,
				onChange: (e) => setQ(e.target.value),
				placeholder: "Filter by name or area…",
				className: "ml-auto rounded-lg bg-surface/70 px-3 py-2 text-[13px] ring-1 ring-line outline-none placeholder:text-mute focus:ring-ember/40"
			})]
		}),
		/* @__PURE__ */ jsxs(Panel, {
			className: "overflow-x-auto p-2",
			delay: 100,
			children: [
				/* @__PURE__ */ jsxs("table", {
					className: "w-full min-w-[820px] text-left text-[13px]",
					children: [/* @__PURE__ */ jsx("thead", { children: /* @__PURE__ */ jsxs("tr", {
						className: "label-mono",
						children: [
							/* @__PURE__ */ jsx("th", {
								className: "p-3 font-normal",
								children: "Vendor"
							}),
							/* @__PURE__ */ jsx("th", {
								className: "p-3 font-normal",
								children: "Category"
							}),
							/* @__PURE__ */ jsx("th", {
								className: "p-3 font-normal",
								children: "Rating"
							}),
							/* @__PURE__ */ jsx("th", {
								className: "p-3 font-normal",
								children: "Added by"
							}),
							/* @__PURE__ */ jsx("th", {
								className: "p-3 font-normal",
								children: "Updated"
							}),
							/* @__PURE__ */ jsx("th", {
								className: "p-3 font-normal",
								children: "Status"
							}),
							/* @__PURE__ */ jsx("th", {
								className: "p-3 font-normal",
								children: "Actions"
							})
						]
					}) }), /* @__PURE__ */ jsx("tbody", {
						className: "divide-y divide-line",
						children: list.map((v) => /* @__PURE__ */ jsxs("tr", {
							className: "transition hover:bg-ink/[0.03]",
							children: [
								/* @__PURE__ */ jsx("td", {
									className: "p-3",
									children: /* @__PURE__ */ jsxs("div", {
										className: "flex items-center gap-3",
										children: [/* @__PURE__ */ jsx(Thumb, {
											src: v.image ?? fallbackVendorImage,
											alt: v.name,
											className: "size-10"
										}), /* @__PURE__ */ jsxs("div", { children: [/* @__PURE__ */ jsx("p", {
											className: "font-medium",
											children: v.name
										}), /* @__PURE__ */ jsx("p", {
											className: "font-mono text-[11px] text-mute",
											children: v.location
										})] })]
									})
								}),
								/* @__PURE__ */ jsx("td", {
									className: "p-3",
									children: v.category
								}),
								/* @__PURE__ */ jsxs("td", {
									className: "p-3 font-mono",
									children: [
										"★ ",
										v.rating.toFixed(1),
										" ",
										/* @__PURE__ */ jsxs("span", {
											className: "text-mute",
											children: [
												"(",
												v.reviewsCount,
												")"
											]
										})
									]
								}),
								/* @__PURE__ */ jsx("td", {
									className: "p-3 font-mono text-mute",
									children: v.addedBy ?? "—"
								}),
								/* @__PURE__ */ jsx("td", {
									className: "p-3 font-mono text-mute",
									children: v.updated
								}),
								/* @__PURE__ */ jsx("td", {
									className: "p-3",
									children: /* @__PURE__ */ jsx(StatusPill, { status: v.status })
								}),
								/* @__PURE__ */ jsx("td", {
									className: "p-3",
									children: v.status === "Verified" ? /* @__PURE__ */ jsx(GhostButton, {
										className: "text-clay",
										disabled: suspendMutation.isPending,
										onClick: () => suspendMutation.mutate(v.id),
										children: "Suspend"
									}) : v.status === "Suspended" ? /* @__PURE__ */ jsx(GhostButton, {
										disabled: unsuspendMutation.isPending,
										onClick: () => unsuspendMutation.mutate(v.id),
										children: "Reinstate"
									}) : null
								})
							]
						}, v.id))
					})]
				}),
				isLoading ? /* @__PURE__ */ jsx("p", {
					className: "p-8 text-center text-mute",
					children: "Loading…"
				}) : null,
				!isLoading && list.length === 0 ? /* @__PURE__ */ jsx("p", {
					className: "p-8 text-center text-mute",
					children: "No vendors match."
				}) : null
			]
		})
	] });
}
//#endregion
export { Vendors as component };
