# Base URL for the NGiLA API. EXPO_PUBLIC_ prefix makes this available at
# runtime via process.env — required by Expo's env var handling.
EXPO_PUBLIC_API_URL=https://ngila-api.azurewebsites.net/api
EXPO_PUBLIC_GOOGLE_MAPS_API_KEY=your-google-maps-api-key
